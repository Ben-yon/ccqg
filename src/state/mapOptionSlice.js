import { createSlice } from 'react-redux';


const initialState = {
    flagCode: '',
    status: "idle"
}


export const mapOptionSlice = createSlice({
    name: 'mapOption',
    initialState,

    reducers: {
        
    }
})